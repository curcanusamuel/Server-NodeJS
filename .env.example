PORT=3000

# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ids_clinic
DB_USER=postgres
DB_PASSWORD=

# SSL/TLS
# For RDS set DB_SSL=true
DB_SSL=false
# Set true only when using trusted CA/certificate setup
DB_SSL_REJECT_UNAUTHORIZED=false
